/**
 * AUTO-GENERATED — do not edit by hand.
 *
 * Populated by `npm run images` (scripts/optimize-images.mjs), which renders
 * responsive AVIF/WebP/JPEG derivatives for every source image and records the
 * resulting `srcSet` strings here, keyed by the original `/images/...` path.
 *
 * Until the pipeline is run this stays an empty object, so `images.js` falls
 * back to the single-JPEG behaviour and nothing points at assets that do not
 * exist yet. Safe to commit empty.
 *
 * @type {Record<string, { srcSet?: string, webp?: string, avif?: string }>}
 */
export const generatedImageSources = {}
